class Solution {
public:
    vector<int> getRow(int rowIndex) {
        vector<int> ans;
        long c = 1;
        ans.push_back(c);
        for(int j = 1; j <= rowIndex; ++j)
        {
            c = (c*(rowIndex - j + 1))/j;
            ans.push_back(c);
        }
        return ans;
    }
};
